# Optional transmitter-side client UDP ingress config.
# UDP payload: 4-byte big-endian bid in cents, followed by delimiter byte 0/1
# and 1..8 body bytes in [2,255].
backend=kernel_udp
listen_ip=0.0.0.0
listen_port=9100
client.0.ip=127.0.0.1
